{% extends 'base.html.twig' %}

{% block body %}
     <div class="card">
        <div class="card-header">
          Crear nuevo mensaje
        </div>
        <div class="card-body">
              {{ form(form, {'attr': {'id': 'formulario_insertar_mensaje'}}) }}
        </div>
     </div>
   
{% endblock %}